{
  "$schema": "../question.schema.json",
  "id": "q-sf-median-order-amount",
  "slug": "sf-median-order-amount",
  "title": "Median order amount by status (Snowflake MEDIAN)",
  "prompt": "You're writing Snowflake SQL. Using Snowflake's MEDIAN aggregate, return the median order amount for each order status. Columns: status, median_amount. Order by status.",
  "difficulty": "easy",
  "packs": [
    "Snowflake",
    "Joins & Aggregations"
  ],
  "dialects": [
    "snowflake"
  ],
  "datasetId": "ecommerce",
  "order": 64,
  "canonical": {
    "generic": [
      "SELECT status, MEDIAN(amount) AS median_amount",
      "FROM orders",
      "GROUP BY status",
      "ORDER BY status"
    ],
    "snowflake": [
      "SELECT status, MEDIAN(amount) AS median_amount",
      "FROM orders",
      "GROUP BY status",
      "ORDER BY status"
    ]
  },
  "grading": {
    "orderMatters": true
  },
  "requires": {
    "pattern": "\\bmedian\\s*\\(",
    "flags": "i",
    "message": "Use Snowflake's MEDIAN aggregate."
  },
  "hints": [
    "Group the orders by status, then take MEDIAN(amount) of each group.",
    "MEDIAN is an aggregate, so it pairs with GROUP BY status."
  ]
}
