{
  "$schema": "../question.schema.json",
  "id": "q-sf-generic-customers",
  "slug": "sf-generic-customers",
  "title": "Customers whose name starts with a prefix (Snowflake STARTSWITH)",
  "prompt": "You're writing Snowflake SQL. Using Snowflake's STARTSWITH function, return the customers whose name begins with 'A'. Columns: customer_id, name. Order by customer_id.",
  "difficulty": "easy",
  "packs": [
    "Snowflake",
    "Strings & regex"
  ],
  "dialects": [
    "snowflake"
  ],
  "datasetId": "ecommerce",
  "order": 65,
  "canonical": {
    "generic": [
      "SELECT customer_id, name",
      "FROM customers",
      "WHERE starts_with(name, 'A')",
      "ORDER BY customer_id"
    ],
    "snowflake": [
      "SELECT customer_id, name",
      "FROM customers",
      "WHERE STARTSWITH(name, 'A')",
      "ORDER BY customer_id"
    ]
  },
  "grading": {
    "orderMatters": true
  },
  "requires": {
    "pattern": "startswith",
    "flags": "i",
    "message": "This question is about STARTSWITH — solve it with that, not LIKE."
  },
  "hints": [
    "STARTSWITH(name, 'A') is TRUE when name begins with that letter.",
    "Use it in the WHERE clause to keep only those rows."
  ]
}
