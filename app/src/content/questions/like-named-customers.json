{
  "$schema": "../question.schema.json",
  "id": "q-like-named-customers",
  "slug": "like-named-customers",
  "title": "Names containing a pattern (LIKE)",
  "prompt": "Using LIKE, return the customers whose name contains 'son' (e.g. Johnson, Thompson, Wilson). Single column name, alphabetical.",
  "difficulty": "easy",
  "packs": [
    "Core SQL Foundations"
  ],
  "dialects": [
    "generic"
  ],
  "datasetId": "ecommerce",
  "order": 53,
  "canonical": {
    "generic": [
      "SELECT name",
      "FROM customers",
      "WHERE name LIKE '%son%'",
      "ORDER BY name"
    ]
  },
  "grading": {
    "orderMatters": true
  },
  "requires": {
    "pattern": "\\blike\\b",
    "flags": "i",
    "message": "This question is about LIKE — use a LIKE pattern."
  },
  "hints": [
    "LIKE matches patterns; % is any run of characters.",
    "A substring match anywhere in the name is WHERE name LIKE '%son%'."
  ]
}
