---
title: Accessibility pass — tabs, row links, live regions, keyboard patterns
type: chore
area: a11y
priority: P2
effort: M
status: open
---

## Problem

Pattern-level gaps (the baseline is otherwise good — toggles carry `aria-pressed`/`aria-checked`, reduced-motion is respected):

- `app/src/components/TopBar.tsx:100` — `aria-label="Practice or Learn"` is stale (three tabs now, incl. Model); the `role="tablist"` tabs lack the required arrow-key roving/`tabIndex` management — either implement the ARIA tabs pattern or drop the tab roles for plain buttons (more honest).
- `app/src/components/ProblemList.tsx:250-258` — `role="link"` on a `<tr>` destroys table semantics for screen readers; handles Enter but not Space. Put a real link/button in the title cell instead.
- `app/src/components/DbtWorkspace.tsx:466-472` — resizer has `role="separator"` but isn't focusable, no keyboard resize, no `aria-valuenow`.
- `DbtWorkspace.tsx:548` — terminal output needs `aria-live="polite"`; same for the SQL-console error (:574).
- `app/src/dev/FeedbackWidget.tsx:23-30` — closes on outside-click but not Escape (`ProfileMenu.tsx:34-36` already does this right — copy the pattern).
- `app/src/components/ProfileMenu.tsx:65` — `role="menu"` without arrow-key item navigation (same gap as the tabs; plain buttons acceptable).

## Acceptance criteria

- Each item addressed or consciously downgraded (e.g. roles removed rather than half-implemented).
- Keyboard-only walkthrough works: switch pillars, open a question from the list, use the profile menu.
- e2e + screenshot suites green.
